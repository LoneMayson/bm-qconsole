&НаСервере
Функция ЗначениеХранилища(КлючЗаписи) Экспорт
	
	Выборка = РегистрыСведений.bm_Запросы.Выбрать(Новый Структура("Идентификатор", КлючЗаписи.Идентификатор));
	Выборка.Следующий();
	Значение = Выборка.Хранилище.Получить();
	Возврат Значение;
	
КонецФункции